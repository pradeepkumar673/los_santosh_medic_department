{
  "name": "mediqueue-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:all": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\" \"cd ai-no-show-service && python -m uvicorn app.main:app --port 8001\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
